import PassportView from '@/sections/passport/view/passport-view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Reasonable Adjustments Passport',
  description: 'View and share your approved reasonable adjustments',
};

export default function PassportPage() {
  return <PassportView />;
}
