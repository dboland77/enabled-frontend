'use client';
import { UserCardsView } from '@/sections/user/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'enableD: User Cards',
};

export default function UserCardsPage() {
  return <UserCardsView />;
}
